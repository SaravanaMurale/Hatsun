<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sosaley.hatsun">

    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/hatsun_agro_pro_logo"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".authentication.OTPActivity" /> <!-- <activity android:name=".authentication.SignupActivity"></activity> -->
        <activity android:name=".menu.QRDisplayActivity" /> <!-- <activity android:name=".register.SplashScreenActivity"></activity> -->
        <activity android:name=".menu.QRScannerActivity" />
        <activity android:name=".authentication.SignupActivity" />
        <activity android:name=".authentication.SigninActivity" />
        <activity android:name=".authentication.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>



</manifest>